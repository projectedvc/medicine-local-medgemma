{
  "model": "google/medgemma-1.5-4b-it",
  "learning_rate": 0.000005,
  "epochs": 1.0,
  "per_device_train_batch_size": 1,
  "per_device_eval_batch_size": 1,
  "gradient_accumulation_steps": 8,
  "warmup_ratio": 0.03,
  "weight_decay": 0.01,
  "max_grad_norm": 1.0,
  "logging_steps": 5,
  "eval_steps": 100,
  "save_steps": 250,
  "save_total_limit": 2,
  "seed": 42,
  "minimum_replay_ratio": 0.25,
  "note": "Full-parameter candidate. Do not run before preflight and manifest validation pass."
}
